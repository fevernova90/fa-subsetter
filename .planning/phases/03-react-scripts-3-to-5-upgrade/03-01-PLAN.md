---
phase: 03-react-scripts-3-to-5-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/App.test.js
  - src/store.test.js
  - src/components/Icons.test.js
  - src/components/AddIconForm.test.js
  - src/components/Generator.test.js
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "react-scripts is at version 5.0.1 in package.json and node_modules"
    - "npm run build completes without errors under Webpack 5"
    - "npm test exits 0 with all frontend tests passing under Jest 27"
    - "react-app-rewired and customize-cra are absent from package.json"
    - "npm audit vulnerability count is reduced by approximately 200 compared to the Phase 1 baseline"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency versions"
      contains: "react-scripts.*5.0.1"
    - path: "src/App.test.js"
      provides: "App smoke test with jsdom environment docblock"
      contains: "@jest-environment jsdom"
    - path: "src/store.test.js"
      provides: "Redux store tests with jsdom environment docblock"
      contains: "@jest-environment jsdom"
    - path: "src/components/Icons.test.js"
      provides: "Icons render test with jsdom environment docblock"
      contains: "@jest-environment jsdom"
    - path: "src/components/AddIconForm.test.js"
      provides: "AddIconForm render test with jsdom environment docblock"
      contains: "@jest-environment jsdom"
    - path: "src/components/Generator.test.js"
      provides: "Generator render test with jsdom environment docblock"
      contains: "@jest-environment jsdom"
  key_links:
    - from: "package.json"
      to: "node_modules/react-scripts"
      via: "npm install"
      pattern: "\"react-scripts\".*\"5\\.0\\.1\""
    - from: "src/*.test.js and src/components/*.test.js"
      to: "jest jsdom environment"
      via: "@jest-environment jsdom docblock"
      pattern: "@jest-environment jsdom"
---

<objective>
Upgrade react-scripts from 3.2.0 to 5.0.1, clearing approximately 200 of 239 npm audit vulnerabilities. Removes unused react-app-rewired and customize-cra. Fixes Jest 27 breaking change (default test environment changed from jsdom to node) by adding per-file docblocks to all 5 frontend test files. Verifies the Webpack 5 build succeeds and all tests pass.

Purpose: SEC-01 — react-scripts 5.0.1 is the final stable CRA release, carries Webpack 5 and Jest 27, and resolves ~200 transitive vulnerabilities in the build toolchain.
Output: Working react-scripts 5.0.1 installation with passing tests and a passing production build.
</objective>

<execution_context>
@/Users/muhaimin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhaimin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/muhaimin/Developer/fa-subsetter/.planning/PROJECT.md
@/Users/muhaimin/Developer/fa-subsetter/.planning/ROADMAP.md
@/Users/muhaimin/Developer/fa-subsetter/.planning/STATE.md

<interfaces>
<!-- Key constraints and facts extracted from codebase. Executor uses these directly. -->

From package.json (current state):
- react-scripts: "3.2.0"
- react: "^16.12.0", react-dom: "^16.12.0"  — stay on React 16, do NOT upgrade
- sass: "^1.98.0"  — dart-sass already installed from Phase 2
- devDependencies includes: react-app-rewired@^2.1.5, customize-cra@^0.9.1 — REMOVE BOTH
- devDependencies includes: supertest@^6.3.4  — can upgrade to ^7.x once Jest 27 is active
- All scripts use "react-scripts" directly (not "react-app-rewired") — no config-overrides.js

Jest 27 breaking change:
- Default testEnvironment changed from "jsdom" to "node"
- Fix: add `/** @jest-environment jsdom */` docblock as the FIRST line of every frontend test file
- Do NOT add it to server/server.test.js (already runs via `test:server` with --testEnvironment node)

Files that need the jsdom docblock:
1. src/App.test.js
2. src/store.test.js
3. src/components/Icons.test.js
4. src/components/AddIconForm.test.js
5. src/components/Generator.test.js

From src/index.js:
- Uses ReactDOM.render() and serviceWorker.unregister() — valid on React 16, no changes needed

Node 24 + Webpack 5:
- react-scripts 5 uses Webpack 5 which resolves the Node 24 OpenSSL legacy provider issue
- NODE_OPTIONS=--openssl-legacy-provider should NOT be needed; only add if build fails with OpenSSL error
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove react-app-rewired/customize-cra and upgrade react-scripts to 5.0.1</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - /Users/muhaimin/Developer/fa-subsetter/package.json — verify current react-scripts version and devDependencies before touching anything
  </read_first>
  <action>
    Run the following commands in sequence from the project root (/Users/muhaimin/Developer/fa-subsetter):

    Step 1 — Remove unused devDependencies (react-app-rewired and customize-cra):
    ```
    npm uninstall react-app-rewired customize-cra --ignore-scripts
    ```

    Step 2 — Upgrade react-scripts to 5.0.1 (pin exactly, not ^5 — this is the final CRA release):
    ```
    npm install react-scripts@5.0.1 --save-exact --ignore-scripts
    ```

    After both commands, verify package.json reflects:
    - "react-scripts": "5.0.1" (exact, no caret) in dependencies
    - react-app-rewired and customize-cra are gone from devDependencies
    - react, react-dom remain at ^16.x — do NOT let npm upgrade them

    If npm tries to upgrade react or react-dom to 18.x, add them as overrides:
    ```json
    "overrides": {
      "react": "^16.12.0",
      "react-dom": "^16.12.0"
    }
    ```

    Also upgrade supertest to 7.x now that Jest 27 handles node: protocol:
    ```
    npm install supertest@^7.0.0 --save-dev --ignore-scripts
    ```

    Use --ignore-scripts throughout to avoid postinstall build steps.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); console.log(p.dependencies['react-scripts']); if(!p.dependencies['react-scripts'].startsWith('5')) process.exit(1);" && node -e "const p=require('./package.json'); if(p.devDependencies['react-app-rewired']||p.devDependencies['customize-cra']) process.exit(1); console.log('rewired/customize removed ok');"</automated>
  </verify>
  <acceptance_criteria>
    - package.json shows react-scripts at 5.0.1
    - react-app-rewired and customize-cra absent from devDependencies
    - react and react-dom still at ^16.x (not upgraded to 18)
    - node_modules/react-scripts/package.json version is 5.0.1
  </acceptance_criteria>
  <done>react-scripts 5.0.1 installed, unused deps removed, react 16 pinned</done>
</task>

<task type="auto">
  <name>Task 2: Fix Jest 27 jsdom environment — add docblocks to all 5 frontend test files</name>
  <files>
    src/App.test.js,
    src/store.test.js,
    src/components/Icons.test.js,
    src/components/AddIconForm.test.js,
    src/components/Generator.test.js
  </files>
  <read_first>
    - /Users/muhaimin/Developer/fa-subsetter/src/App.test.js
    - /Users/muhaimin/Developer/fa-subsetter/src/store.test.js
    - /Users/muhaimin/Developer/fa-subsetter/src/components/Icons.test.js
    - /Users/muhaimin/Developer/fa-subsetter/src/components/AddIconForm.test.js
    - /Users/muhaimin/Developer/fa-subsetter/src/components/Generator.test.js
  </read_first>
  <action>
    Jest 27 changed the default testEnvironment from "jsdom" to "node". All 5 frontend test files use ReactDOM.render() or DOM APIs and will crash without jsdom. Fix by prepending the per-file docblock to each file.

    For EACH of the 5 files, insert `/** @jest-environment jsdom */` as the very first line (before any imports), followed by a blank line. The docblock must appear before any code or import statements for Jest to pick it up.

    Final first two lines of each file must look exactly like:
    ```
    /** @jest-environment jsdom */

    import React from 'react';
    ...
    ```

    Do NOT modify server/server.test.js — it runs via npm run test:server with --testEnvironment node already set.

    Do NOT add a jest.config.js or jest field in package.json. The per-file docblock approach is preferred because server/server.test.js needs node environment, not jsdom.

    After editing all 5 files, run the tests:
    ```
    CI=true npx react-scripts test --watchAll=false
    ```

    If tests fail with `ReferenceError: document is not defined` → docblock is missing or malformed on the failing test file.
    If tests fail with `Cannot find module` → react-scripts install incomplete; re-run npm install.
    If tests fail with `act(...)` warnings but tests still pass → acceptable, warnings are non-fatal.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 5 frontend test files start with `/** @jest-environment jsdom */` as line 1
    - `CI=true npx react-scripts test --watchAll=false` exits 0
    - Test output shows at least 8 passing tests (3 store tests + 1 App test + 1 Icons + 1 AddIconForm + 1 Generator)
    - No test file shows "Your test suite must contain at least one test" error
    - grep confirms: `grep -l "@jest-environment jsdom" src/App.test.js src/store.test.js src/components/Icons.test.js src/components/AddIconForm.test.js src/components/Generator.test.js | wc -l` outputs 5
  </acceptance_criteria>
  <done>All 5 frontend tests pass under Jest 27 with jsdom environment</done>
</task>

<task type="auto">
  <name>Task 3: Verify Webpack 5 build succeeds and audit improvement is confirmed</name>
  <files>package.json</files>
  <read_first>
    - /Users/muhaimin/Developer/fa-subsetter/package.json — check current state after Tasks 1 and 2
  </read_first>
  <action>
    Step 1 — Run the production build:
    ```
    npm run build
    ```

    Expected: build/ directory created, exit 0, no errors. Webpack 5 (bundled with react-scripts 5) handles Node 24 natively — do NOT set NODE_OPTIONS=--openssl-legacy-provider unless you see an explicit OpenSSL error.

    If build fails with "digital envelope routines::unsupported" (OpenSSL error on older Webpack):
    - This means react-scripts 5.0.1 was not properly installed
    - Re-run: npm install react-scripts@5.0.1 --save-exact --ignore-scripts
    - Then retry npm run build

    If build fails with a CSS/SCSS import error:
    - Check server/server.js still uses sass.compile() (dart-sass) — not node-sass
    - The build uses the react-scripts sass loader which auto-detects the `sass` package

    Step 2 — Run npm audit and capture the count:
    ```
    npm audit 2>&1 | tail -5
    ```

    Expected: vulnerability count significantly lower than the Phase 1 baseline of 239. A reduction of ~200 is expected. If total remaining is under 50, the upgrade worked correctly.

    Step 3 — If residual transitive vulnerabilities remain in nth-check, postcss, glob-parent, semver, word-wrap, or tough-cookie, add npm overrides to package.json to silence them:
    ```json
    "overrides": {
      "nth-check": "^2.1.1",
      "postcss": "^8.4.31"
    }
    ```
    Only add overrides for packages that actually appear in `npm audit --json` output. Do not add preemptive overrides.

    Step 4 — Run server tests to confirm no regression:
    ```
    npm run test:server
    ```

    Step 5 — Commit all changes with message:
    feat(03-react-scripts-3-to-5-upgrade): upgrade react-scripts 3→5, fix Jest 27 jsdom env
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm audit 2>&1 | tail -3 && npm run test:server 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and produces build/ directory
    - `npm audit` total vulnerability count is below 50 (down from 239 baseline)
    - `npm run test:server` exits 0 with 3 passing tests
    - No OpenSSL errors during build
    - build/static/js/ contains at least one .js chunk file (Webpack 5 output present)
  </acceptance_criteria>
  <done>Webpack 5 build passes, audit shows ~200 fewer vulnerabilities, server tests still green</done>
</task>

</tasks>

<verification>
Overall phase verification (run after all 3 tasks complete):

```bash
# 1. Check react-scripts version
node -e "const p=require('./node_modules/react-scripts/package.json'); console.log('react-scripts:', p.version); if(p.version !== '5.0.1') process.exit(1);"

# 2. Confirm removed deps are gone
node -e "const p=require('./package.json'); if(p.devDependencies && (p.devDependencies['react-app-rewired'] || p.devDependencies['customize-cra'])) { console.error('FAIL: unused deps still present'); process.exit(1); } console.log('unused deps removed ok');"

# 3. Confirm all 5 frontend test files have jsdom docblock
grep -l "@jest-environment jsdom" src/App.test.js src/store.test.js src/components/Icons.test.js src/components/AddIconForm.test.js src/components/Generator.test.js | wc -l
# Expected output: 5

# 4. All tests pass
CI=true npx react-scripts test --watchAll=false

# 5. Build passes
npm run build

# 6. Audit reduced
npm audit 2>&1 | tail -3
```
</verification>

<success_criteria>
Phase 3 is complete when ALL of the following are true:

1. `node_modules/react-scripts/package.json` shows version `5.0.1`
2. `npm run build` exits 0 — Webpack 5 build produces build/ directory
3. `CI=true npx react-scripts test --watchAll=false` exits 0 — all frontend tests pass under Jest 27
4. `npm run test:server` exits 0 — 3 server smoke tests still pass
5. `npm audit` reports fewer than 50 total vulnerabilities (down from 239 baseline)
6. `react-app-rewired` and `customize-cra` absent from package.json devDependencies
</success_criteria>

<output>
After completion, create `/Users/muhaimin/Developer/fa-subsetter/.planning/phases/03-react-scripts-3-to-5-upgrade/03-01-SUMMARY.md` using the summary template.
</output>
